<?xml version="1.0"?>
<ruleset name="Luna Date Coding Standard">
    <description>Coding standard for Luna Date package</description>

    <!-- Use PSR-12 as base -->
    <rule ref="PSR12"/>

    <!-- Include source and test directories -->
    <file>src/</file>
    <file>tests/</file>

    <!-- Exclude vendor directory and simple test -->
    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>tests/simple_test.php</exclude-pattern>

    <!-- Additional rules -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="140"/>
        </properties>
    </rule>

    <!-- Ensure docblocks are present -->
    <rule ref="Generic.Commenting.DocComment">
        <exclude-pattern>tests/</exclude-pattern>
    </rule>

    <!-- Type declarations -->
    <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
        <properties>
            <property name="newlinesCountBetweenOpenTagAndDeclare" value="1"/>
            <property name="spacesCountAroundEqualsSign" value="0"/>
        </properties>
    </rule>
</ruleset>
